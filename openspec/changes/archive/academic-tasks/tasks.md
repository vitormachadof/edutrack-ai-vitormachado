## 1. Modelagem de Dados

- [ ] 1.1 Criar tabela `academic_tasks` com colunas: `id`, `title`, `description`, `due_date`, `status`, `subject_id`.
- [ ] 1.2 Adicionar coluna `user_id` para vincular cada tarefa ao aluno proprietário.
- [ ] 1.3 Definir `subject_id` como referência para a tabela `subjects` existente.
- [ ] 1.4 Adicionar índices em `subject_id`, `user_id` e `due_date` para consultas eficientes.
- [ ] 1.5 Incluir campos de auditoria padrão: `created_at` e `updated_at`.
